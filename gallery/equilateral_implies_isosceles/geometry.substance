-- Theorem: equilateral_implies_isosceles
Point A, B, C

Segment AB := Segment(A, B)
Segment BC := Segment(B, C)
Segment CA := Segment(C, A)

-- Constraints:
EqualLength(AB, BC)
EqualLength(BC, CA)
EqualLength(AB, CA)

-- Labels:
AutoLabel A, B, C
